<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rails Starter" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Rails Starter">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-screen">
    <% if flash[:alert] %>
      <div class="bg-red-50 border-l-4 border-red-500 text-red-700 p-4 mb-4" role="alert">
        <p><%= flash[:alert] %></p>
      </div>
    <% end %>
    <% if flash[:notice] %>
      <div class="bg-green-50 border-l-4 border-green-500 text-green-700 p-4 mb-4" role="alert">
        <p><%= flash[:notice] %></p>
      </div>
    <% end %>

    <main class="flex-grow">
      <%= yield %>
    </main>

    <%# Footer Section %>
    <footer class="w-full -mx-5 px-5 border-t border-gray-200 py-12 bg-gray-50">
      <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-8">
        <div class="text-center md:text-left">
          <div class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
            <%= t(".footer.branding") %>
          </div>
          <p class="text-gray-600 text-sm mt-1">
            <%= t(".footer.tagline") %>
          </p>
        </div>

        <div class="flex gap-6 flex-wrap justify-center">
          <%= link_to t(".footer.documentation"), "https://github.com/jademind/ralph-rails-starter", class: "text-gray-600 hover:text-blue-600 transition-colors font-medium" %>
          <%= link_to t(".footer.github"), "https://github.com/jademind/ralph-rails-starter", class: "text-gray-600 hover:text-blue-600 transition-colors font-medium" %>
          <%= link_to t(".footer.issues"), "https://github.com/jademind/ralph-rails-starter", class: "text-gray-600 hover:text-blue-600 transition-colors font-medium" %>
          <%= link_to t(".footer.changelog"), "https://github.com/jademind/ralph-rails-starter", class: "text-gray-600 hover:text-blue-600 transition-colors font-medium" %>
        </div>
      </div>

      <div class="text-center mt-8 text-sm text-gray-500">
        © <%= Time.current.year %> Rails Starter. Built with ❤️ using Rails 8.1
      </div>
    </footer>

  </body>
</html>
